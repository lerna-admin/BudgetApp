# Proyecto: budgetapp
- Este proyecto tiene su propia carpeta: ~/projects/budgetapp
- Variables sensibles en: ~/projects/budgetapp/.env (no se comparte con otros proyectos).
- Logs en: ~/projects/budgetapp/logs/logs.jsonl (no se comparten entre proyectos).
- Si algún recurso falta, el comando `proj` lo recreará automáticamente al seleccionar el proyecto.
