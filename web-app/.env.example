# URL base usada por la app web para consumir la API (apuntar al mock si el backend no está disponible)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4010

# URL pública de la aplicación (para links/callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000
